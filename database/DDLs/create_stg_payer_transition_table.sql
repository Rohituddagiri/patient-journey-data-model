create table stg_payerTransitions(
    patientID VARCHAR,
    memberID VARCHAR,
    startDate VARCHAR,
    endDate VARCHAR,
    payerID VARCHAR,
    secondarypayer VARCHAR,
    planOwnership VARCHAR,
    ownerName VARCHAR
);