create table stg_medications(
    start VARCHAR,
    stop VARCHAR,
    patientID VARCHAR,
    payerID VARCHAR,
    encounterID VARCHAR,
    code VARCHAR,
    description VARCHAR,
    baseCost VARCHAR,
    payerCoverage VARCHAR,
    dispenses VARCHAR,
    totalCost VARCHAR,
    reasonCode VARCHAR,
    reasonDescription VARCHAR
);