create table stg_organizations(
    organizationID VARCHAR,
    name VARCHAR,
    address VARCHAR,
    city VARCHAR,
    state VARCHAR,
    zip VARCHAR,
    lat VARCHAR,
    lon VARCHAR,
    phone VARCHAR,
    revenue VARCHAR,
    utilization VARCHAR,
    PRIMARY KEY(organizationID)
);